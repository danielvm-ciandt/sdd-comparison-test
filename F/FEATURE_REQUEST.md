# Feature Request: User Authentication

Add user authentication so only logged-in users can access their tasks.

Users should be able to sign up and log in. Once logged in, they should only
see their own tasks, not tasks from other users.
